import React, { FunctionComponent } from "react";
import { Modal, IIconProps } from 'office-ui-fabric-react';

const NewRoom:FunctionComponent = () => {

    const cancelIcon: IIconProps = { iconName: 'Cancel' };

    return <>
            <Modal>
dfadfsad
            </Modal>
        </>
}

export default NewRoom;
